// const BASEURL = import.meta.env.BASE_URL || "localhost:5000"

const URLs = {
    getUsers: "https://jsonplaceholder.typicode.com/users",
    postUser: "https://jsonplaceholder.typicode.com/posts"
}

export default URLs;